# Booth 4 — Cóc Nhập Ngũ 2026

Web app kiosk (React + Vite + TypeScript + Tailwind). No backend. Follow BRD and Tech Spec first, then the UI/UX system.

## Always read

- `BRD-Booth4-Mat-Ma-So-4.md`
- `TECH-SPEC-Booth4-Mat-Ma-So-4.md`
- `.cursor/skills/booth4-ui-ux/SKILL.md` for any screen, component, token, or copy work

## Rules

Project rules in `.cursor/rules/` encode the same system: `ui-ux-core` (always), `ui-components` (`*.tsx`/`*.css`), `ui-copy` (`*.ts`/`*.tsx`).

## Non-negotiables

- Client-only; no user data store; origin separate from Card Battle
- Mobile-first 390×844, 48px targets, no keyboard for metrics
- Oswald + Be Vietnam Pro, brass-on-olive, dog-tag + ammo-belt
- Never “Fertility Score” / “Reproductive Health Score”
